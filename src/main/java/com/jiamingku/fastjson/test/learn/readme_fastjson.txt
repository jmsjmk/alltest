1.转换时候尽量用引用类型,不要用简单类型,null 属性尽量都输出（String toJSONString(Object object, boolean prettyFormat)）

2.在进行反序列化的时候，应该一步到位，不应该出现中间过度对象JSONObject对象（一步到位)

3.其实数值类的类型,--我所说的数值类型就是value里面不带双引号这种类型
3.1 类型,都可以转换-只是有的会截短
3.2 如果value 被双引号括起来之后，会容易出现异常类型不相符等错误原因

4.异常其实也没什么的，不就是一个对象么，有啥的，对象里面带着一个数组，就是一个序列化的过程

5.边缘的问题
"", "null",null 序列化转换对应的类型,包括字符串在内都是null对象  ---直接还原成为java对象但是在作为属性的时候就会出现问题?

注意 "null",作为一个属性的时候在在进行反序列化的时候会出现特殊情况-
1.接受对象是是具体的对象比如user-会出现格式不正确
2.Object 接受本质就是一个字符串
3.String 就是长度=4

"" 空串同样也有同样的问题
如果是对象，非object那种就是null了
如果string 长度=0 的字符串
如果object 其实就是string 长度=0的字符串
